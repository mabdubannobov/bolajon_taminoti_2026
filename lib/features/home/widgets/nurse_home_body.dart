import 'package:flutter/material.dart';

class NurseHomeBody extends StatelessWidget {
  const NurseHomeBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Hamshira uchun maxsus ekran (Statistika)"),
    );
  }
}
