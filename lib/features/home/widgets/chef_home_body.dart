import 'package:flutter/material.dart';

class ChefHomeBody extends StatelessWidget {
  const ChefHomeBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Oshpaz uchun maxsus ekran (Bannerlar)"));
  }
}
